import React from 'react';

interface Props {
    className?: string;
}

const NavMobile: React.FC<Props> = ({className}) => {
    return (
        <div className={className}>
            
        </div>
    );
};

export default NavMobile;